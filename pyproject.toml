[project]
name = "memory-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.27.1",
    "azure-data-tables>=12.5.0",
    "python-dotenv>=1.0.0",
]
