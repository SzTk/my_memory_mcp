# Azure Functions 用依存パッケージ
# Azure Functions ランタイム (Python 3.11) がこのファイルを読んでインストールする。
# ローカル開発は pyproject.toml + uv を使うこと。

azure-functions
azure-data-tables>=12.5.0
python-dotenv>=1.0.0
mcp[cli]>=1.27.1

# OAuth 2.1 / Google OIDC 対応
PyJWT[crypto]>=2.8.0
requests>=2.28.0
